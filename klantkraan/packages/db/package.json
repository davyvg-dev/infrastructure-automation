{
  "_ctx7": "context7: drizzle-orm /drizzle-team/drizzle-orm-docs (2026-05-20), drizzle-kit_0.31.5 (2026-05-20). neon-http driver for Cloudflare Workers — HTTP only, no WebSocket.",
  "name": "@kk/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "drizzle-kit generate",
    "migrate": "node --experimental-strip-types src/migrate.ts",
    "lint": "echo 'lint: configure eslint in a follow-up'"
  },
  "dependencies": {
    "@kk/domain": "workspace:*",
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.36.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.5",
    "typescript": "^5.7.2"
  }
}
