{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".astro/**"],
      "env": ["CLIENT", "NODE_ENV"],
      "inputs": ["$TURBO_DEFAULT$", "clients/**"]
    }
  }
}
