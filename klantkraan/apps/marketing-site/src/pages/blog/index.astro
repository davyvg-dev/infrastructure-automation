---
// context7: Astro Content Collections — getCollection (/llmstxt/astro_build_llms-full_txt, 2026-05-20)
import { getCollection } from 'astro:content'
import Base from '../../layouts/Base.astro'

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
)

// Covers every vertical the blog collection allows (src/content/config.ts).
const verticalLabel: Record<(typeof posts)[number]['data']['vertical'], string> = {
  loodgieter: 'Loodgieters',
  dakdekker: 'Dakdekkers',
  schilder: 'Schilders',
  installateur: 'Installateurs',
  elektricien: 'Elektriciens',
  aannemer: 'Aannemers',
  algemeen: 'Algemeen',
}

const dateFmt = new Intl.DateTimeFormat('nl-NL', {
  day: 'numeric',
  month: 'long',
  year: 'numeric',
})
---

<Base
  title="Blog — Klantkraan"
  description="Praktische artikelen voor Nederlandse loodgieters en dakdekkers over AI-receptionist, gemiste oproepen, no-shows en klantenmotor-automatisering."
>
  <section class="py-8">
    <h1 class="font-display text-hero text-(--color-kraan-ink)">Blog</h1>
    <p class="mt-6 max-w-2xl text-h3 text-(--color-kraan-stone-700)">
      Eerlijke artikelen over gemiste oproepen, AI-telefoniste en automatisering voor Nederlandse
      vakmensen. Geen marketing-praatjes, wel cijfers.
    </p>
  </section>

  <section class="grid gap-4 md:gap-6 py-8 md:grid-cols-2">
    {
      posts.map((post) => (
        <article class="flex flex-col rounded-(--radius-card) border border-(--color-kraan-stone-300) bg-(--color-kraan-cream) p-5 md:p-6">
          <header>
            <p class="text-micro uppercase tracking-wide text-(--color-kraan-stone-500)">
              {verticalLabel[post.data.vertical]} · {dateFmt.format(post.data.pubDate)}
            </p>
            <h2 class="mt-3 font-display text-h2 text-(--color-kraan-ink)">
              <a
                href={`/blog/${post.id.replace(/\.md$/, '')}/`}
                class="text-(--color-kraan-ink) no-underline hover:text-(--color-kraan-blue)"
              >
                {post.data.title}
              </a>
            </h2>
          </header>
          <p class="mt-4 flex-1 text-body text-(--color-kraan-stone-700)">
            {post.data.description}
          </p>
          <a
            href={`/blog/${post.id.replace(/\.md$/, '')}/`}
            class="mt-6 inline-flex min-h-[2.75rem] items-center text-body font-medium text-(--color-kraan-blue) no-underline hover:underline"
          >
            Lees het artikel →
          </a>
        </article>
      ))
    }
  </section>
</Base>
