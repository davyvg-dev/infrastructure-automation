# Agent regressions

Numbered incident log, appended by `/log-regression` — date, client, channel, symptom, root cause, evidence pointer, fix direction. Every entry gets a matching golden in `ai-receptionist/app/evals.py`.
