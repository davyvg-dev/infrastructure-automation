# Dev/test dependencies. Install with: pip install -r requirements-dev.txt
-r requirements.txt
ruff==0.16.0   # pinned: formatter output is version-sensitive, so CI and local must match
pytest>=8      # offline unit tests (tests/)
